[package]
name = "mazle-eval"
version = "0.1.0"
edition = "2021"

[lib]
name = "mazle_eval"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
mazle-generator = { path = "../.." }
